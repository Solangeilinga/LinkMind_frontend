/// Sur mobile natif (Android/iOS), il n'y a pas de barre d'adresse — rien à
/// configurer. Ce stub existe uniquement pour que l'appel dans main.dart
/// compile identiquement sur toutes les plateformes.
void configurePathUrlStrategy() {}
